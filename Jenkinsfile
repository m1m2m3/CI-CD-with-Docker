pipeline{
    agent any
    stages{
       stage('SCM checkout')
            {
            steps{ 
                git 'https://github.com/m1m2m3/CI-CD-with-Docker.git'
                 }
             }
    
         }
    
    
    
}
